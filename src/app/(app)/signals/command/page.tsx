import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function CommandCenterPage() {
  return (
    <PlaceholderPage
      title="Signal Intelligence Command"
      subtitle="Cross-layer correlation matrix and signal source health dashboard"
      phase="Phase 3 -- Dashboard & Signal Screens"
    />
  );
}
